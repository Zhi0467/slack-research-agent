/iterative-review {REVIEW_ROUNDS_ARGS}

Development task: implement backlog item {ITEM_ID}. Read the BACKLOG entry in `docs/dev/BACKLOG.md` and linked plan/issue files in `docs/dev/plans/` and `docs/dev/issues/`.
{ADDITIONAL_CONTEXT}
Post progress updates in the Slack thread using thread_ts `{THREAD_TS}` in channel `{CHANNEL_ID}`. Update when you start, when you hit milestones, and when you finish.
